[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "wiki"
version = "1.0"

[project.scripts]
wiki = "pipeline.cli:cli"

[tool.setuptools.packages.find]
include = ["pipeline*"]
